# ml/sentiment — host-only dependencies (GPU training + inference)
# Python 3.13 compatible
#
# STEP 1 — Install PyTorch with CUDA first (RTX 4060 → CUDA 12.1):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
#
# STEP 2 — Install the rest:
#   pip install -r ml/sentiment/requirements.txt

# Deep learning (torch installed separately via CUDA wheel — see above)
transformers>=4.40.0
accelerate>=0.29.0

# Data — py312 has wheels for both 1.x and 2.x; stay <2 to match mlflow 2.13 constraint
numpy>=1.26.0,<2.0.0
pandas>=2.2.0
pyarrow>=15.0.0
scikit-learn>=1.5.0

# Iceberg + MLflow (same as project stack)
pyiceberg[s3fs,pandas]==0.6.0
mlflow==2.13.0
boto3>=1.26.0

# Viz / metrics
matplotlib>=3.8.0
seaborn>=0.13.0
scipy>=1.13.0
