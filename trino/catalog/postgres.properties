# Trino PostgreSQL catalog — external metadata for federation queries
# Example: JOIN iceberg.gold.fct_orders WITH postgres.public.marketing_campaigns

connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/olist_meta
connection-user=olist
connection-password=${ENV:POSTGRES_PASSWORD}
