# Credentials — never commit
.env

# Dataset CSVs (download from Kaggle separately)
data/*.csv
data/*.zip

# Spark JARs (download via spark/jars/README.md)
spark/jars/*.jar

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# ML model checkpoints (large binary files — keep local only)
ml/sentiment/checkpoints/**/*.pt
ml/sentiment/checkpoints/**/*.bin
ml/sentiment/checkpoints/confusion_matrices/
output.png

# MLflow
mlruns/

# Claude Code
.claude/

# OS
.DS_Store
Thumbs.db
tunnel-urls.txt
