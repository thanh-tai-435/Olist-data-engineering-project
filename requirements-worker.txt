# Prefect worker dependencies — installed in the worker image

# Core pipeline
pandas==2.2.0
pyarrow==15.0.0
pyiceberg[s3fs,pandas]==0.6.0
confluent-kafka==2.3.0

# PySpark (local[*] mode mặc định; cluster khi SPARK_MASTER được set)
# Version phải khớp với bitnami/spark:3.5 image
pyspark==3.5.1

# ML
mlflow==2.13.0
scikit-learn==1.4.0
xgboost==2.0.3

# Quality
soda-core-duckdb==3.3.0

# Utilities
boto3==1.34.0
requests==2.31.0
setuptools==69.5.1
